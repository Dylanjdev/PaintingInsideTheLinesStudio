# React + Vite
https://paintingoutsidethelinesstudios.com/

## Deployment

Hosted on Cloudflare Pages, auto-deployed from the `main` branch of this repo.

- Build command: `npm run build`
- Build output directory: `dist`
- Push to `main` to trigger a production deploy; other branches get preview deploys.
